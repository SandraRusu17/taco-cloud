logging:
  level:
    tacos: DEBUG

server:
  port: 9090

taco:
  orders:
    pageSize: 10

---
spring:
  profiles: prod

    datasource:
      url: jdbc:mysql://localhost/tacocloud
      username: tacouser
      password: tacopassword

  logging:
    level:
      tacos: WARN